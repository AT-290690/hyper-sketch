in ( range (1; 30); -> (x; i; print(
  ? (== (% (x; 15); 0); "FizzBuzz";
  ? (== (% (x; 3); 0); "Fizz"; 
  ? (== (% (x; 5); 0); "Buzz";  x))))))
  
++ (:=(x; 1); 30; => (print(
  ? (== (% (x; 15); 0); "FizzBuzz";
  ? (== (% (x; 3); 0); "Fizz"; 
  ? (== (% (x; 5); 0); "Buzz";  x))));
  +=(x)))